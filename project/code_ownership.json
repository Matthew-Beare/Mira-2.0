{
  "schema_version": 1,
  "production_roots": [
    {
      "path": "mira",
      "profile": "python",
      "suffixes": [".py"]
    }
  ],
  "components": [
    {
      "id": "mira-package-surface",
      "responsibility": "Stable public Python package export surface for implemented MIRA core components.",
      "why_separate": "Keeps import compatibility and package presentation separate from mutable-state, routing, transport, and repository-governance implementations.",
      "owned_paths": ["mira/__init__.py"],
      "feature_ids": ["CORE-001"],
      "work_ids": ["CODE-OWNERSHIP-001"],
      "verification": ["tests/test_synthetic_roundtrip.py"]
    },
    {
      "id": "structured-state",
      "responsibility": "Provider-neutral canonical structured-state contract and deterministic synthetic adapter semantics.",
      "why_separate": "Storage mutation, revision, replay, query, and event semantics are reusable below authority routing and API transport.",
      "owned_paths": ["mira/structured_state.py"],
      "feature_ids": ["STORE-001"],
      "work_ids": ["STORE-ADAPTER-001A"],
      "verification": ["tests/test_structured_state.py", "tests/test_synthetic_roundtrip.py"]
    },
    {
      "id": "google-sheets-state",
      "responsibility": "Google Sheets REST persistence gateway and single-writer StructuredStateAdapter implementation for the M2-M0 provider-backed authority path.",
      "why_separate": "Provider-specific tab/row persistence and credential injection stay behind the provider-neutral STORE-001 contract so Authority Registry and API code remain unaware of spreadsheet identifiers or ranges.",
      "owned_paths": ["mira/google_sheets_store.py"],
      "feature_ids": ["STORE-001"],
      "work_ids": ["GOOGLE-STORE-ADAPTER-001"],
      "verification": ["tests/test_google_sheets_store.py"]
    },
    {
      "id": "authority-registry",
      "responsibility": "Persistent one-authority-per-data-class registry, fail-closed runtime bootstrap, and explicit runtime adapter resolution.",
      "why_separate": "Canonical authority selection and safe restart bootstrap are an integrity boundary independent of storage implementation and client/API transport.",
      "owned_paths": ["mira/authority.py", "mira/runtime_bootstrap.py"],
      "feature_ids": ["AUTH-001"],
      "work_ids": ["AUTHORITY-REGISTRY-001"],
      "verification": ["tests/test_authority.py", "tests/test_runtime_bootstrap.py", "tests/test_synthetic_roundtrip.py"]
    },
    {
      "id": "api-boundary",
      "responsibility": "Same-user API policy, canonical routing, audit, scoped client sessions, pluggable bearer authentication, and bounded HTTP transport.",
      "why_separate": "Client-facing policy and transport form one service boundary above canonical authority/storage while remaining provider-neutral and deployment-independent.",
      "owned_paths": ["mira/api_core.py", "mira/http_transport.py"],
      "feature_ids": ["API-001"],
      "work_ids": ["API-CORE-001"],
      "verification": ["tests/test_api_core.py", "tests/test_http_transport.py", "tests/test_synthetic_roundtrip.py"]
    },
    {
      "id": "managed-runtime-assembly",
      "responsibility": "Provider-neutral managed API composition and startup readiness verification over injected state, authentication, and audit dependencies.",
      "why_separate": "Deployment composition must verify the existing Authority/API/HTTP graph without embedding provider credentials, resource identifiers, or platform-specific hosting policy into reusable core modules.",
      "owned_paths": ["mira/runtime.py"],
      "feature_ids": ["API-001"],
      "work_ids": ["API-DEPLOYMENT-001"],
      "verification": ["tests/test_runtime.py"]
    },
    {
      "id": "cloud-run-deployment",
      "responsibility": "Cloud Run service identity, restart-stable injected bearer authentication, HTTPS proxy adaptation, rate/audit controls, and live Google deployment composition for the bounded M2-M0 proof.",
      "why_separate": "Cloud-specific identity and hosting behavior must remain outside provider-neutral API/runtime semantics while still being directly tested and fail-closed before live provider writes.",
      "owned_paths": [
        "mira/cloud_run.py",
        "mira/cloud_run_entrypoint.py",
        "mira/google_runtime_auth.py",
        "mira/managed_auth.py"
      ],
      "feature_ids": ["API-001", "STORE-001"],
      "work_ids": ["API-DEPLOYMENT-001"],
      "verification": [
        "tests/test_cloud_run.py",
        "tests/test_google_runtime_auth.py",
        "tests/test_managed_auth.py"
      ]
    },
    {
      "id": "workspace-first-run",
      "responsibility": "Public Google Workspace starter bundle integrity for the zero-infrastructure Personal MIRA path, including runtime-only copy binding and direct validation of the bound Apps Script artifact.",
      "why_separate": "Workspace packaging and browser-first deployment constraints are adapter/distribution concerns and must not leak provider IDs or Google execution behavior into the provider-neutral API and storage core.",
      "owned_paths": ["mira/workspace_bundle.py"],
      "feature_ids": ["API-001", "STORE-001", "ONBOARD-006"],
      "work_ids": ["API-DEPLOYMENT-001A"],
      "verification": ["tests/test_workspace_bundle.py"]
    },
    {
      "id": "workspace-native-client",
      "responsibility": "Deterministic single-writer preflight, replay, revision, atomic batch mutation and exact-readback protocol for stock ChatGPT using authenticated native Google Sheets actions.",
      "why_separate": "The Personal native-connector lane needs a durable integrity contract that matches STORE-001 without turning the model, Apps Script, or Google transport into a second canonical data model.",
      "owned_paths": ["mira/workspace_native.py"],
      "feature_ids": ["API-001", "AUTH-001", "STORE-001"],
      "work_ids": ["CHATGPT-API-CLIENT-001"],
      "verification": ["tests/test_workspace_native.py"]
    },
    {
      "id": "canonical-command-sequencer",
      "responsibility": "Provider-neutral one-at-a-time queued command execution with durable replay, stale-revision conflict, crash-before-acknowledgement recovery, and API-001 authorization semantics for the Android-era shared writer boundary.",
      "why_separate": "Multi-client mutation requires one canonical sequencing seam above Authority/store semantics. Keeping the command sequencer provider-neutral lets Google Workspace or a managed runtime supply execution without making Android depend on spreadsheet rows or Cloud Run details.",
      "owned_paths": ["mira/command_sequencer.py"],
      "feature_ids": ["CLIENT-ANDROID-001", "API-001", "AUTH-001", "STORE-001"],
      "work_ids": ["ANDROID-CLIENT-CORE-001"],
      "verification": ["tests/test_command_sequencer.py"]
    },
    {
      "id": "personal-firstboot",
      "responsibility": "Provider-neutral four-question Minimum Useful Setup and durable resumable Interview Ledger for the stock-ChatGPT Personal MIRA front door.",
      "why_separate": "Onboarding question order, answer normalization, completion truth, and explicit provider-preference semantics are product-domain behavior above storage and must remain independent of Google row coordinates, Android, and service activation implementations.",
      "owned_paths": ["mira/onboarding.py"],
      "feature_ids": ["ONBOARD-002", "ONBOARD-003", "CORE-001", "SERVICE-001", "CAL-006", "STUDIO-001"],
      "work_ids": ["FIRSTBOOT-CORE-001"],
      "verification": ["tests/test_onboarding.py"]
    },
    {
      "id": "repository-integrity",
      "responsibility": "Machine validation of canonical feature dependencies, work-session direction, and production component ownership/evidence coverage.",
      "why_separate": "Repository integrity gates govern source growth and CI evidence rather than participating in mutable product-state authority or runtime provider behavior.",
      "owned_paths": [
        "mira/feature_registry.py",
        "mira/code_ownership.py",
        "mira/work_session_alignment.py"
      ],
      "feature_ids": ["DEV-005", "DEV-006", "DEV-007"],
      "work_ids": ["FEATURE-REGISTRY-001", "CODE-OWNERSHIP-001", "FEATURE-ALIGN-001"],
      "verification": [
        "tests/test_feature_registry.py",
        "tests/test_code_ownership.py",
        "tests/test_work_session_alignment.py"
      ]
    }
  ]
}
