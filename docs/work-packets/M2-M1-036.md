# M2-M1-036 — Studio guided user-facing orchestration surface

## Packet identity

- **Packet ID:** `M2-M1-036`
- **Primary work:** `MIRA-STUDIO-001`
- **Primary features:** `STUDIO-001`, `DEV-004`, `DIST-001`
- **Related invariants/features:** `DEV-002`, `DEV-005`, `DEV-006`, `DEV-007`, `SOURCE-001`, `PROVIDER-001`, `RECOVERY-002`
- **Repository:** `Matthew-Beare/Mira-2.0`
- **Branch:** `work/m2-m1-036-studio-guided-surface`
- **Base SHA:** `16c5646e05edb62b3878ade602a3287d12c064f4`
- **Pull request:** `#151`

## Dependency evidence

M2-M1-031 through M2-M1-033 provide the merged/test-verified bounded Studio skill-builder seams: competitive candidate evidence/review planning, staged preview/test/rollback/approval planning, and approved activation/rollback execution through injected source adapters.

M2-M1-034 and M2-M1-035 provide the merged/test-verified controlled-sharing seams: deterministic sanitized package validation, inert compatibility/dependency inspection, explicit publication authorization, provider-neutral transport, exact readback/recovery semantics and untrusted import retrieval. M2-M1-035 merged at `16c5646e05edb62b3878ade602a3287d12c064f4`; exact post-merge CI #588 plus trusted-runner preflight passed.

The remaining immediate `MIRA-STUDIO-001` gap is therefore not another lower-level engine. It is a bounded product-level lifecycle surface that tells a user where one Studio change stands, why it is blocked or ready, and what action is actually permitted next without smuggling in approval or execution authority.

## Objective

Implement a deterministic, provider-neutral MIRA Studio session/surface model for one bounded preference/workflow/feature change. Compose existing evidence and execution receipts into explicit user-visible phases and next actions while preserving hard separation among preview/review, explicit activation approval, activation execution, rollback and optional sharing/import review.

## Out of scope

- model invocation or prompt execution;
- branch/worktree creation or candidate code generation;
- provider selection or credentials;
- live source/share-store calls;
- automatic activation or rollback;
- automatic install of imported behavior;
- final graphical/browser UI;
- Android/Workspace/finance/People Discovery mutation.

## Acceptance evidence

1. **Bounded exact session identity — PASS.** `StudioSession` binds session/packet/work/change/kind/objective plus sorted canonical feature/dependency IDs and rejects contradictory identity.
2. **Reuse of reviewed evidence authority — PASS.** `review_studio_change()` delegates readiness and exact approval binding to existing `evaluate_staged_change()` rather than duplicating preview/test/rollback semantics.
3. **Deterministic phase/next-action surface — PASS.** `StudioSurfacePhase` and `StudioNextAction` cover draft, review-blocked, approval, activation, recovery, rollback and import-review states with deterministic SHA-256 projection.
4. **No activation without exact approval plan — PASS.** Review-ready unapproved changes remain `AWAITING_APPROVAL`; only an approval-bound `StudioActivationPlan` reaches `READY_TO_ACTIVATE`.
5. **Truthful activation/rollback projection — PASS.** Supplied activation/rollback receipts are bound back to the exact approved plan and map APPLIED/ALREADY_APPLIED, BLOCKED and RECOVERY_REQUIRED without manufacturing success.
6. **Sharing remains separate — PASS.** A verified active change may expose optional share-review readiness, but the surface never publishes, transports or infers sharing from activation.
7. **Imported material remains inert — PASS.** `inspect_studio_import()` reuses independent feature-share validation/compatibility inspection and always emits review-required or blocked state with install/source-mutation/activation authority false.
8. **Public product surface remains provider/runtime neutral — PASS.** Direct tests assert the public session/surface/import dataclasses expose no credential/token/secret/endpoint/host/IP/MAC/shell/command/runner-label/model-path fields; provider/source target receipts are consumed but never exposed by the surface.
9. **Deterministic replay — PASS.** Identical reviewed or import evidence produces equal surfaces and equal projection digests.
10. **Adversarial coverage — PASS.** `tests/test_studio.py` contains 21 direct tests covering missing/failed evidence, stale approval, mismatched identity, activation blocking/recovery, rollback outcomes, inert imports and surface invariants.
11. **Bounded ownership and repository CI — PASS at implementation head.** `studio-guided-surface` owns only `mira/studio.py` with `tests/test_studio.py`; CI #590 / run `34562064822` on exact head `811c66302c1ad37af6e6ab187c2d8a77b4bf1f0c` passed compile, feature registry, product lifecycle ledger, Personal starter distribution, work-session alignment, code ownership, Android gates, Python unit tests and Workspace tests end-to-end.

## Backlog lifecycle reconciliation

`BACKLOG.md` still carries stale queued text for `SKILL-BUILDER-001`, `FEATURE-SHARE-001`, and `MIRA-STUDIO-001`. This implementation packet does not rewrite those high-contention lifecycle rows before its own integration state exists. After exact merge-SHA verification, the next governance/Studio selection packet must reconcile the first two to their already-proven merged state and advance `MIRA-STUDIO-001` only to the evidence actually earned by M2-M1-036. Branch-local `CURRENT_WORK.md` remains the exact active-work authority until then.

## Evidence ceiling

Implementation and repository tests prove only deterministic product orchestration/state projection over supplied existing evidence. They do not prove live model invocation, provider/source/share execution, final graphical UX, or imported installation/activation.

## Exact next action

1. Close branch documentation on the implementation/ownership evidence above.
2. Run final exact-head CI after documentation closeout.
3. Re-read remote `main`, PR #151 exact head/mergeability and changed-file overlap.
4. If exact-head CI remains green with no incompatible main movement, mark ready and merge with expected-head protection.
5. Read back exact merge SHA and require post-merge repository CI plus trusted-runner/preflight success before claiming integration verification.
6. Start the next bounded Studio/governance packet from that merge SHA to reconcile backlog lifecycle state and select the next user-visible Studio child.
