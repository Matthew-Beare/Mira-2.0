# M2-M1-035 — Feature-share publication/import transport

## Packet identity

- **Packet ID:** `M2-M1-035`
- **Primary work:** `FEATURE-SHARE-001`
- **Primary features:** `DIST-001`, `STUDIO-001`, `DEV-004`, `SOURCE-001`
- **Repository:** `Matthew-Beare/Mira-2.0`
- **Branch:** `work/m2-m1-035-feature-share-transport`
- **Base SHA:** `b792d11f6717f6aefe9edabcf65ed2fcc9a36492`

## Prior packet evidence

`M2-M1-034` merged through PR #149 at `b792d11f6717f6aefe9edabcf65ed2fcc9a36492`. Exact post-merge CI #579 / run `34522059866` passed end-to-end. That packet proves deterministic sanitized package construction, independent integrity/privacy validation, and inert import inspection. It does not prove publication/import transport.

## Objective

Close the remaining bounded `FEATURE-SHARE-001` transport seam without choosing or hard-coding a provider. Add an injected share-store boundary that can publish an already-validated sanitized package with explicit user authorization, require exact remote readback of the same package bytes/identity, retrieve an imported package as untrusted bytes, independently revalidate it, and return inert review material only.

This packet must not activate imported behavior, mutate MIRA source, install artifacts, choose a public registry, expose provider credentials/endpoints, infer approval from publication, or claim a live provider implementation.

## Acceptance criteria

1. Publication starts only from a package that independently passes `mira.feature_share` validation.
2. Publication requires explicit user authorization bound to the exact package ID and destination namespace.
3. The transport adapter is injected and provider-neutral; public contracts contain no credentials, host identities, provider endpoints, shell commands or private resource IDs.
4. Publication success requires adapter write plus exact remote readback whose canonical bytes and package ID match the authorized package.
5. Write/readback uncertainty is reported as recovery-required rather than fabricated success or clean rollback.
6. Replay of an already remotely identical package is zero-write and deterministic.
7. Import retrieval treats provider bytes as untrusted, validates schema/privacy/digests/package identity independently, then performs compatibility/dependency inspection.
8. Imported packages remain inert: retrieval/review never grants install, source mutation or activation authority.
9. Destination/package identity mismatch, malformed transport receipts, stale/foreign authorization and provider tampering fail closed.
10. Tests cover success, replay, denied/stale authorization, write failure/unknown outcome, remote mismatch/tamper, malformed import, dependency/compatibility findings and authority separation.
11. Exact-head repository CI passes before merge; exact post-merge CI is required for integration verification.

## Evidence ceiling

Specified and branch-open only. No live publication/import transport or activation is claimed.

## Exact next action

Implement provider-neutral transport/reconciliation in `mira/feature_share_transport.py`, add adversarial tests, register one bounded ownership component, run exact-head CI, then close and merge only from verified evidence.
