# M2-M1-040 — Ops mutable-fact authority and receipt-to-inventory reliability

## Trigger

On 2026-09-12, an Ops Brief reported a shipment ETA without live carrier readback. The customer then supplied a false ETA as a test; MIRA accepted it despite an existing tracking number. The customer also challenged whether forwarded receipt/order email was reliably being converted into durable receipt evidence plus canonical non-consumable inventory with receipt linkage and categorization.

## Objective

Make mutable operational facts fail closed against the correct fact-specific authority and make durable non-consumable purchase ingestion atomic enough that MIRA cannot claim success after only reading an email.

## Repository

`Matthew-Beare/Mira-2.0`

## Branch

`work/m2-m1-040-ops-evidence-authority`

## Base SHA

`6551691c7836a9c70a8be080cd11bb2ee18536d2`

## Owned surfaces

- `mira/authority.py` — provider-neutral fail-closed mutable-fact and durable-purchase authority policy, colocated with the existing canonical Authority boundary
- `tests/test_ops_evidence_policy.py`
- `docs/OPS_EVIDENCE_AUTHORITY_CONTRACT.md`
- this packet document
- this branch's `CURRENT_WORK.md`
- live AM/PM brief automation prompts, as operational deployment state outside public Git

## Shared/high-contention surfaces

`CURRENT_WORK.md` only. The packet deliberately does not edit `project/code_ownership.json`: open Studio PR #153 owns a non-overlapping additive change there. The new policy was moved into the already-owned `mira/authority.py` boundary so the reliability repair does not manufacture a future ownership-manifest merge conflict.

## Scope

1. Shipment ETA/status with a tracking number requires live carrier readback before it can be reported as verified current state.
2. Chat, memory, vendor email, stale projections, and user guesses cannot silently replace a reachable third-party live authority.
3. User corrections remain authoritative for user-controlled/observed facts, but do not automatically override external provider state.
4. Durable owned non-consumables require receipt evidence, durable archive/link, category, canonical asset/inventory record, receipt linkage, and successful readback before ingestion is marked complete.
5. Consumables remain purchase/receipt/spend evidence and are not forced into durable inventory.
6. Existing inventory/asset/receipt authorities are reused; no new private database or duplicate spreadsheet is created.

## Out of scope

- migrating historical receipt archives wholesale;
- changing financial accounting authority;
- changing Studio intake behavior;
- inventing missing product identity/category/location data;
- scraping carrier sites through unsupported bypasses;
- committing private shipment, receipt, email, account, or spreadsheet identifiers to the public repo.

## Acceptance criteria

1. With tracking present and no carrier readback, a conflicting user/vendor ETA does not produce a verified ETA. **PASS.**
2. With tracking present and live carrier readback, carrier state wins over conflicting user/vendor values. **PASS.**
3. A durable purchase does not commit from receipt email evidence alone. **PASS.**
4. A durable purchase fails closed if the canonical record receipt link differs from the archived receipt link. **PASS.**
5. A durable purchase commits only after the full archive/category/canonical-record/link/readback chain succeeds. **PASS.**
6. Consumables do not require durable inventory records. **PASS.**
7. AM and PM brief runtime prompts explicitly enforce the live-carrier and receipt-to-inventory contracts. **PASS — deployed and read back 2026-09-12.**
8. Branch state and exact head are read back after writes. **PASS for pre-merge checkpoint; integration verification remains.**

## Evidence

- Isolated deterministic policy test run: `6 passed`.
- Live Workspace inspection confirmed the existing canonical surfaces already support this workflow: the purchase/receipt archive contains physical-asset identity and relationship tables; the tool inventory exposes order-number and receipt-link fields. The packet therefore reuses existing authorities instead of creating another store.
- Enabled `MIRA AM Brief` automation was updated and read back with `MUTABLE LIVE FACT AUTHORITY — MANDATORY` and `RECEIPT → INVENTORY COMMIT — MANDATORY` sections. It now requires live carrier dereference when tracking exists and uses the existing purchase/receipt archive, tool inventory, and inventory index roles.
- Enabled `MIRA PM Brief` automation was updated and read back with the same two mandatory contracts.
- The deployed prompts explicitly state that receipt/order email is evidence only, durable non-consumables are not complete until archive + canonical inventory/asset + receipt-link + category/relationship + readback agree, and consumables are not forced into durable inventory.
- Current `main` remained at base SHA `6551691c7836a9c70a8be080cd11bb2ee18536d2` during the initial pre-integration checks.
- Open Studio PR #153 initially overlapped this packet on `CURRENT_WORK.md` only. Its ownership-manifest patch adds only `studio-intake`; this packet intentionally avoided editing that manifest after CI exposed the new unowned module.
- PR #155 CI #605 failed at work-session alignment because the first branch checkpoint lacked required alignment metadata. The metadata was repaired; CI #606 then passed alignment and failed at code ownership because `mira/ops_evidence_policy.py` was a new unregistered production file.
- The code-ownership failure was fixed without weakening the gate: policy code was colocated with the already-owned `mira/authority.py` Authority boundary, tests were retargeted, and the redundant unowned module was deleted. No ownership-manifest exception or bypass was added.

## Exact next action / resume point

1. Refresh PR #155 exact head after this packet evidence checkpoint.
2. Require exact-head CI green through code ownership, Python unit tests, Android checks, and Workspace tests.
3. Re-read current `main` immediately before merge; if compatible, merge with expected-head protection.
4. Read back post-merge `main` and post-merge CI before claiming Git integration complete.
5. Preserve/reconcile Studio PR #153 rather than overwriting its concurrent `CURRENT_WORK.md` or ownership-manifest change.

## Evidence ceiling

This packet proves deterministic policy behavior, Git durability, and prompt deployment/readback. It cannot prove a carrier provider will always expose package-specific data to every runtime; when carrier readback is unavailable, the required behavior is explicitly `UNVERIFIED`, not fabrication.
