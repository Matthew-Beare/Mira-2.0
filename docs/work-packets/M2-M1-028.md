# M2-M1-028 — Deterministic compute hardware-safety policy

## Packet identity

- **Packet ID:** `M2-M1-028`
- **Primary work:** `LOCAL-INTEGRATIONS`
- **Primary features:** `LOCAL-001`
- **Related invariants/features:** `OBS-001`, `RECOVERY-002`, `API-001`
- **Branch:** `work/m2-m1-028-hardware-safety`
- **Base SHA:** `20aa43c99e03cef8e9aa80817890d3484ea0f04f`
- **Repository:** `Matthew-Beare/Mira-2.0`
- **Pull request:** `#143`

## Dependency evidence

`M2-M1-027` merged as PR #142 at `20aa43c99e03cef8e9aa80817890d3484ea0f04f`. Post-merge CI #542 passed end-to-end on that exact main SHA. M2-M1-027 therefore closes at implemented/test/integration-verified observability-contract evidence only; no live Prometheus/Grafana/private sensor deployment was claimed.

## Objective

Implement the provider-neutral deterministic safety-decision seam that later private sensor adapters and lifecycle executors can consume. Explicit configured sensor policies plus fresh observations produce a reproducible normal/warning/critical/sensor-fault decision and bounded response requirements without an LLM inventing thresholds or directly controlling hardware.

This packet is policy/evidence only. It does not read a real host sensor, call IPMI/NVML/Redfish, stop a process, drain a real scheduler, power off a machine, send Wake-on-LAN, modify worker registry state, or claim a live safe-shutdown path.

## Configuration contract

Each configured required sensor declares:

- a generic sensor ID used only inside the private/runtime configuration boundary;
- an explicit unit token;
- whether higher or lower values are worse;
- an explicit warning threshold;
- an explicit critical threshold;
- a positive freshness window.

Threshold ordering is validated against direction:

- `high_is_bad`: warning is strictly below critical;
- `low_is_bad`: warning is strictly above critical.

No default CPU/GPU temperature, fan RPM, pump RPM, coolant temperature, power, voltage or other hardware threshold is supplied by public product code. Threshold values are configuration/evidence, not model guesses.

The node-level policy explicitly configures whether a critical reading requests safe shutdown and whether a missing/stale required sensor requests safe shutdown. Those booleans are never inferred from a machine name or hardware type.

## Observation contract

A sensor observation contains only sensor ID, numeric finite value, unit and UTC observed-at time. Evaluation receives an explicit UTC `now`.

- duplicate observations for one sensor fail closed;
- an observation for an unconfigured sensor is ignored rather than silently becoming safety authority;
- future configured observations are malformed and fail validation;
- required sensors with no observation are `sensor_fault`;
- required observations older than their configured freshness window are `sensor_fault`;
- unit mismatch is `sensor_fault` rather than comparing incompatible values.

## Severity semantics

Per-sensor severity is deterministic:

- `normal`: within configured safe side of warning;
- `warning`: warning threshold crossed but critical threshold not crossed;
- `critical`: critical threshold crossed;
- `sensor_fault`: required evidence is missing, stale, or unit-incompatible.

The node decision uses the most conservative aggregate class. `sensor_fault` and `critical` are both stop/fault classes; they remain distinguishable for provenance and operator diagnosis.

Exact threshold equality is conservative and deterministic: equality to a warning threshold is `warning`, and equality to a critical threshold is `critical`, in either high-is-bad or low-is-bad direction.

## Response requirements

This packet returns response requirements only. It executes nothing.

- `normal`: new work may be admitted; no drain/stop/fault/shutdown requirement.
- `warning`: do not admit new work and request drain; do not stop current workloads or fault the node solely from warning.
- `critical`: do not admit new work, request drain, require affected workloads to stop, and require node fault state. Safe shutdown is required only when explicitly enabled by node policy.
- `sensor_fault`: same conservative stop/fault behavior as critical because required safety evidence is unavailable. Safe shutdown is required only when explicitly enabled for sensor faults.

When critical and sensor-fault conditions coexist, safe-shutdown requirement is the deterministic OR of the two explicit policy booleans. A later power/lifecycle packet owns execution, interactive/do-not-wake/do-not-shutdown lock reconciliation, provider-specific shutdown/WOL adapters and durable action readback. This packet cannot bypass those boundaries by itself.

## Evidence/provenance rules

The evaluation result retains:

- policy ID;
- evaluated-at time;
- aggregate severity;
- deterministic sorted reason codes;
- one bounded per-sensor result with configured direction/unit, evidence state, observed value/time when valid, age, and severity;
- computed response requirements.

It does not contain hostnames, IPs, MACs, serial numbers, credentials, model paths, raw shell commands or private provider resource IDs.

## Production ownership

`project/code_ownership.json` contains exactly one new `compute-hardware-safety` component owning `mira/compute_safety.py`, referencing `LOCAL-001` and `RECOVERY-002`, anchored to `LOCAL-INTEGRATIONS`, and requiring direct verification through `tests/test_compute_safety.py`.

The base-to-implementation-head comparison confirmed this manifest change is exactly nine added lines with no unrelated manifest deletion or rewrite.

## Verification evidence

- Branch created from exact verified main/base `20aa43c99e03cef8e9aa80817890d3484ea0f04f`.
- Draft PR #143 opened from `work/m2-m1-028-hardware-safety`.
- Pre-CI implementation head `379abae1952b4ccc84b9c08d26a0c6095b4064d0` changed exactly five intended files: branch-local `CURRENT_WORK.md`, packet doc, `mira/compute_safety.py`, `tests/test_compute_safety.py`, and `project/code_ownership.json`.
- CI #543 on `379abae1952b4ccc84b9c08d26a0c6095b4064d0` passed compile, feature registry, product lifecycle and starter distribution, then failed at work-session alignment because branch metadata used singular `Primary feature` instead of the required machine-parsed `Primary features`. Ownership, Android and Python gates were skipped. No implementation failure was observed.
- Corrective commit `c7d84a8a79a32b9ec2c2619edfb4af377ae4e8a5` changed only that branch metadata field label.
- CI #544 on corrected implementation head `c7d84a8a79a32b9ec2c2619edfb4af377ae4e8a5` passed end-to-end: compile, feature registry, product lifecycle, starter distribution, work-session alignment, code ownership, Android proof/provenance/retention, Python unit tests and Workspace Apps Script tests all succeeded.
- Direct tests cover policy validation, duplicate policy/observation IDs, non-finite thresholds, both threshold directions, exact warning/critical boundaries, warning/critical/fault response requirements, explicit shutdown flags, missing/stale/future/unit-mismatch evidence, exact freshness boundary, unknown observation non-authority, simultaneous critical+fault behavior, input-order independence, bounded evidence shape and normal-state no-action semantics.
- No live hardware-safety, shutdown, sensor, private collector or private deployment evidence is claimed.

## Concurrency state

Old draft PR #134 remains unfinished People Discovery work and has a stale ownership-manifest edit. M2-M1-028 uses current main as authority and its manifest change is narrowly limited to its own component. PR #134 must later reconcile with current main rather than constraining current integrity work.

## Exact resume point

1. Run one final full CI on the documentation-closeout head created after CI #544.
2. Re-read remote `main`, PR #143 head/mergeability and changed-file overlap.
3. Mark PR #143 ready and merge only if the final exact-head CI is green, using expected-head protection.
4. Read back merged `main` and verify post-merge CI before claiming integration verification.
5. Keep the evidence ceiling at implemented/test/integration-verified safety-policy contracts only; do not claim live sensor or shutdown behavior.
