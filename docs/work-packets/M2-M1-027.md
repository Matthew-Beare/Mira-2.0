# M2-M1-027 — Compute observability foundation

## Packet identity

- **Packet ID:** `M2-M1-027`
- **Primary work:** `LOCAL-INTEGRATIONS`
- **Primary features:** `OBS-001`, `LOCAL-001`
- **Related invariants/features:** `RECOVERY-002`, `PROVIDER-001`, `STORE-001`
- **Branch:** `work/m2-m1-027-compute-observability`
- **Base SHA:** `d17dbed73d6fc55a7bd77dbd5bc2fa1e166b9499`
- **Repository:** `Matthew-Beare/Mira-2.0`
- **Pull request:** `#142`

## Objective

Implement a provider-neutral read-only observability projection for the compute fabric using existing validated worker and compute-job views as source truth. Produce deterministic Prometheus-compatible metrics plus a Grafana-compatible PromQL panel plan without creating a second state authority or leaking private/high-cardinality identifiers.

This packet does not deploy Prometheus, Grafana, exporters on private machines, a scrape endpoint, alert delivery, hardware sensor collectors, cooling/power thresholds, or live private infrastructure.

## Source contracts

- `mira.service_state.WorkerRegistryView` is the worker-state source.
- `mira.command_sequencer.ComputeJobView` is the job-state source.
- Observability aggregates and renders those views but never mutates or persists replacement worker/job truth.

## Implemented metric surface

The implementation emits deterministic snapshot gauges for:

- worker counts by bounded runtime/availability/health/identity/mode/interactive-lock state;
- worker heartbeat age maximum and stale-worker count by runtime kind;
- job counts and summed attempts by lifecycle state;
- maximum queued-job age;
- terminal job lifecycle duration count/sum/max by terminal state;
- terminal result counts by state and local/hosted/unknown runtime kind.

The renderer uses deterministic ordering and Prometheus text escaping. These remain snapshot gauges rather than pretending mutable aggregate observations are monotonic counters.

## Privacy/cardinality rules

Metric labels and dashboard query variables in this packet never include:

- worker ID;
- job ID;
- principal ID;
- lane ID;
- runtime ID;
- artifact IDs or hashes;
- hostname/IP/MAC/serial;
- model path/model secret;
- credential/token material;
- raw operation/input/error text.

Only bounded aggregate state dimensions are exposed. Runtime kind is `local`, `hosted`, or derived `unknown` for terminal result reconciliation. Stable identifiers are used internally only to reconcile terminal job result worker identity against current worker registry truth.

## Time semantics

- All input timestamps and `now` are explicit UTC ISO-8601 values.
- Future worker heartbeats, future job creation/start/finish times, negative durations and malformed timestamps fail closed.
- Worker staleness is a deterministic configured freshness window; it is observability evidence only and does not independently mutate worker health/availability.
- Queue age is measured only for queued jobs from `created_at` to `now`.
- Terminal lifecycle duration is wall-clock `created_at` to `finished_at`, not fabricated active CPU/GPU duration.
- The packet does not infer paused/running active compute duration from incomplete lifecycle history.

## Grafana-compatible query plan

The module exposes stable read-only PromQL expressions for:

1. total workers;
2. ready workers;
3. faulted workers;
4. stale worker count;
5. jobs by state;
6. oldest queued-job age;
7. failed jobs;
8. terminal jobs by runtime kind.

The query plan is configuration/projection only. It is not a deployed dashboard and never becomes state authority.

## Ownership

`project/code_ownership.json` now contains exactly one `compute-observability` component owning `mira/compute_observability.py`, referencing `OBS-001` and `LOCAL-001`, anchored to `LOCAL-INTEGRATIONS`, and requiring direct verification through `tests/test_compute_observability.py`.

The base-to-implementation-head comparison confirmed the manifest change is exactly nine added lines and no unrelated manifest material changed.

## Verification evidence

- Branch created from exact verified main/base `d17dbed73d6fc55a7bd77dbd5bc2fa1e166b9499`.
- Draft PR #142 opened from `work/m2-m1-027-compute-observability`.
- Implementation head `cd22c1e1b715575fbd9df28b8e9dc52c5ba5e31e` changed exactly five intended files: branch-local `CURRENT_WORK.md`, packet doc, `mira/compute_observability.py`, `tests/test_compute_observability.py`, and `project/code_ownership.json`.
- CI #539 on exact implementation head `cd22c1e1b715575fbd9df28b8e9dc52c5ba5e31e` passed end-to-end: compile, feature registry, product lifecycle, starter distribution, work-session alignment, code ownership, Android proof/provenance/retention, Python unit tests, and Workspace Apps Script tests all succeeded.
- The direct tests cover deterministic input ordering, worker heartbeat age/staleness, future-heartbeat rejection, complete job-state counts, summed attempts, queue age, terminal lifecycle duration, local/hosted/unknown runtime reconciliation, orphan result workers, malformed lifecycle rejection, duplicate stable-ID rejection, stable Prometheus family ordering, and identifier/privacy exclusion in both Prometheus and Grafana material.
- No live Prometheus, Grafana, scrape endpoint, private collector, hardware sensor, private deployment or alert-delivery evidence is claimed.

## Dependency evidence

`M2-M1-026` merged as PR #141 at `d17dbed73d6fc55a7bd77dbd5bc2fa1e166b9499`. CI #538 passed on that merge commit. Trusted Runner Gate run #1 then passed and emitted a retained receipt for upstream run #538 with source/policy SHA `d17dbed73d6fc55a7bd77dbd5bc2fa1e166b9499`, `source_trust=trusted_canonical_main`, `runner_decision=requires_isolation`, `requires_isolation=true`, and `self_hosted_execution_authorized=false`.

## Concurrency state

Old draft PR #134 also edits `project/code_ownership.json` but is non-mergeable against current main and belongs to unfinished People Discovery work. M2-M1-027 uses current main as authority and adds only its own explicit component entry. PR #134 must later reconcile rather than constraining current compute-fabric integrity work.

## Exact resume point

1. Run one final full CI on the documentation-closeout head created after CI #539.
2. Re-read remote `main`, PR #142 head/mergeability and changed-file overlap.
3. Mark PR #142 ready and merge only if the final exact-head CI is green, using expected-head protection.
4. Read back merged `main` and verify post-merge CI before claiming integration verification.
5. Keep the evidence ceiling at implemented/test/integration-verified observability contracts only; do not claim live Prometheus/Grafana/private sensor deployment.
