# M2-M1-047 — Historical finance reconciliation closure

## Packet identity

- **Packet:** `M2-M1-047`
- **Primary work:** `FIN-CANON-AUDIT-001`
- **Follow-on work:** `FIN-EVIDENCE-RECONCILE-001`
- **Primary features:** `FIN-001`, `FIN-HISTORY-001`, `RECEIPT-001`, `PAYMENT-001`
- **Related integrity/features:** `RECOVERY-001`, `RECOVERY-002`, `MAIL-001`, `ORDER-001`, `ORDER-003`, `ASSET-001`, `ASSET-002`
- **Repository:** `Matthew-Beare/Mira-2.0`
- **Branch:** `work/m2-m1-047-history-reconcile-visible-vertical`
- **Base SHA:** `8ed16f9e2b3e2672ff7149488ba0a7d28a4a0222`
- **Predecessor:** `M2-M1-046`, merged to `main` at the base SHA with post-merge workflow success verified.

## Customer objective

Reconcile the user's trustworthy accessible financial history into canonical MIRROR state instead of treating the beginning of MIRA 2.0 as the beginning of financial reality.

September 1, 2026 may be used as the start boundary for a new recurring reconciliation process, but it is **not** a historical exclusion boundary. If a trustworthy connected source exposes older history, MIRA reconciles it. Existing historical evidence is preserved; unavailable gaps remain explicit; no old production data is deleted or rewritten merely to simplify the model.

After this finance-history blocker is closed or bounded only by actual source unavailability, the next customer-priority implementation objective is the existing Android inventory/scanning vertical using `INV-001`, `INV-002`, `MOVE-001`, `IDENT-001`, `ASSET-001`, `ASSET-002`, `ASSET-003` and the shared Android client architecture.

## Bounded scope

This packet owns the remaining pre-projection finance-history gap only. It will:

1. read actual provider history coverage before making completeness claims;
2. identify the earliest trustworthy accessible transaction history and the lower bound of the current canonical projection;
3. reconcile every accessible pre-projection provider transaction through stable provider identity into the existing canonical ledger/model;
4. match or relate historical mail/order/receipt/refund evidence where support is sufficient without creating duplicate economic events;
5. preserve old evidence and provenance rather than deleting or replacing it;
6. leave unsupported or unavailable source gaps explicit and reviewable;
7. rerun duplicate event/evidence/entity/relation and broken-endpoint invariants;
8. update the finance coverage/readiness projection so MIRA can state exactly what history is canonical;
9. close `FIN-CANON-AUDIT-001` only if historical coverage is complete at the actual accessible-source ceiling.

This packet does **not** implement the Android scanning UI. It records that as the next customer-priority vertical after finance-history closure so it cannot disappear behind more plumbing.

## Owned implementation/data surfaces

- Private connected finance provider history and existing private Financial Escape/MIRROR finance surfaces, subject to exact authority/readback rules.
- Sanitized proof/checkpoint documentation only in public Git.
- `CURRENT_WORK.md` and this packet document on this branch.

## Shared/high-contention surfaces

- `CURRENT_WORK.md` is branch-local recovery state.
- `BACKLOG.md`, `FEATURES.md`, `ROADMAP.md`, and `PROJECT_INSTRUCTIONS.md` are not modified unless a genuine uncaptured requirement is found. Existing feature/work IDs already cover the requested finance-history and inventory/scanning behavior.

## Safety and integrity constraints

- Provider transactions remain money authority.
- Email, orders, receipts and marketplace evidence explain meaning and never become duplicate spend.
- Stable provider transaction identity is required for historical economic-event admission.
- Historical production evidence is preserved. No destructive cleanup, silent truncation, or artificial start date is permitted.
- Provider freshness/coverage ceilings remain explicit.
- Public Git receives sanitized counts/claims only, never private balances, transaction IDs, account IDs, order IDs, receipt contents, addresses or credentials.

## Current verified evidence

- `M2-M1-046` merged to `main` at `8ed16f9e2b3e2672ff7149488ba0a7d28a4a0222`; PR #163 is merged and its post-merge Trusted Runner Gate succeeded.
- Connected-finance transaction coverage reports full history while provider freshness remains UNKNOWN.
- Earliest accessible posted provider history is 2024-08-09.
- The canonical FinOps Ledger currently has 886 represented provider transaction IDs and begins at 2026-01-02.
- Private stable-ID comparison against the entire accessible provider slice proves exactly 1,194 unique posted transactions before the canonical lower bound and zero overlap with the current FinOps Ledger.
- The exact missing set consists of 354 transactions from 2024-08-09 through 2024-12-31 and 840 transactions from 2025-01-01 through 2025-12-31.
- Provider classification distribution confirms the historical set contains both spend and non-spend semantics, including large groups of marketplace purchases, payroll/income, fuel, credit-card payments/transfers and other existing canonical categories. Historical admission must therefore preserve transfer/income exclusions rather than treating every row as spend.
- No private transaction/account identifiers, amounts, merchants or row content are committed to this public packet. The exact identity set exists only in private provider/runtime state for admission/readback.

## Acceptance criteria

1. Predecessor merge and post-merge repository state are verified. **PASS.**
2. Connected provider history coverage is read directly, including earliest available history and freshness/coverage limitations. **PASS — full history; earliest 2024-08-09; freshness UNKNOWN.**
3. Every accessible provider transaction older than the current canonical projection lower bound is identified by stable provider identity. **PASS — 1,194 unique missing IDs; zero overlap with the current ledger.**
4. Every accessible pre-projection provider transaction is either reconciled by stable provider identity or recorded as an explicit bounded exception; none is silently ignored because it predates MIRA 2.0. **PENDING — exact set measured, canonical admission/readback remains.**
5. Historical evidence is preserved and related where supported; no evidence is deleted or overwritten merely to simplify reconciliation. **PASS so far; PENDING closeout verification.**
6. Historical receipt/order/refund evidence cannot create a second economic event for an already represented provider transaction. **PENDING admission/evidence reconciliation.**
7. Duplicate Event IDs, Evidence IDs, Entity IDs and Relation IDs remain zero; broken relation endpoints remain zero after reconciliation. **PENDING post-admission verification.**
8. The canonical finance surface states its actual historical coverage boundary and any unavailable gaps truthfully. **PENDING write/readback.**
9. `FIN-CANON-AUDIT-001` closes only if the accessible historical provider/evidence boundary is fully reconciled and verified; otherwise the remaining blocker is exact and source-bounded. **PENDING.**
10. Sanitized proof is committed to public Git with no private operational identifiers. **PASS so far.**
11. Exact-head CI/PR/merge and post-merge verification complete before the packet is closed. **PENDING.**
12. On closure, next-work selection explicitly prioritizes the Android inventory/scanning vertical unless a higher-ranked integrity/safety blocker is discovered. **LOCKED customer priority.**

## Admission plan

The 1,194-row historical set is too large to admit as one opaque mutation without weakening recovery/readback. Admit it in bounded date/identity batches through the existing canonical schema:

1. preserve provider transaction ID as stable economic-event identity;
2. preserve posted date, account context, signed money semantics, provider classification/confidence and raw provenance;
3. apply existing deterministic non-spend rules for transfers, card repayments and income before spend semantics;
4. carry provider-supported category meaning conservatively and leave merchant/item/necessity ambiguity in the existing review path rather than guessing;
5. relate receipt/order/refund evidence only after exact identity support exists;
6. read back inserted Event IDs and dependent canonical projections after each batch;
7. require duplicate Event/Evidence/Entity/Relation IDs = 0 and broken relation endpoints = 0 after every stage;
8. update historical-coverage boundary only after the earliest admitted row and final row count are independently read back.

## Exact next action / resume point

1. Admit the exact 1,194-row pre-projection set in bounded, read-backable batches through stable provider identity.
2. Start with the earliest provider-history slice so the canonical lower bound moves monotonically backward and recovery remains obvious.
3. Preserve transfer/income/non-spend treatment and existing evidence; do not create duplicate spend.
4. Relate historical evidence only where supported and retain unresolved meaning in the existing review queue.
5. Rerun finance/evidence/entity/relation integrity gates after each bounded stage and at final closure.
6. Update sanitized historical coverage proof and decide closure of `FIN-CANON-AUDIT-001`.
7. On closure, move next to the Android inventory/scanning vertical.

### Idea/backlog capture audit

CAPTURE AUDIT COMPLETE

- Historical reconciliation reuses existing `FIN-CANON-AUDIT-001`, `FIN-001`, `FIN-HISTORY-001`, `RECEIPT-001` and `PAYMENT-001` semantics.
- Android inventory/scanning reuses existing `CLIENT-ANDROID-001`, `INV-001`, `INV-002`, `MOVE-001`, `IDENT-001`, `LOC-001`, `ASSET-001`, `ASSET-002` and `ASSET-003` semantics.
- No new parallel finance authority, history model, inventory model or scanning feature ID is introduced.
