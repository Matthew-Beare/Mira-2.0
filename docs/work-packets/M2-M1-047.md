# M2-M1-047 — Canonical finance historical boundary closure

## Packet identity

- **Packet:** `M2-M1-047`
- **Primary work:** `FIN-CANON-AUDIT-001`
- **Repository:** `Matthew-Beare/Mira-2.0`
- **Branch:** `work/m2-m1-047-finance-history-boundary`
- **Base SHA:** `8ed16f9e2b3e2672ff7149488ba0a7d28a4a0222`
- **Predecessor:** `M2-M1-046`, merged at the base SHA with post-merge CI #657 green.

## Objective

Resolve the final explicit blocker preventing closure of `FIN-CANON-AUDIT-001`: connected-provider transaction history predating the current canonical FinOps projection lower bound.

First quantify the actual pre-projection provider history and reconcile it against existing canonical source identities. Then choose only between two evidence-backed outcomes:

1. **Backfill:** admit supported historical provider transactions into the existing canonical finance model using stable provider identity, preserving transfer/payment semantics and exact readback; or
2. **Durable boundary:** if product policy intentionally excludes older history, encode a clear canonical lower-bound policy with provenance and make every dependent metric/query honest about that boundary.

No silent truncation, no duplicate economic events, and no claim of full historical coverage while the provider and canonical projection disagree.

## Boundaries

- Provider transactions remain money authority.
- Existing canonical events, entities, relations, review overrides, receipts and assets are preserved; no wholesale rebuild or destructive migration.
- Private balances, account IDs, transaction IDs, merchant-level historical details and raw provider payloads never enter public Git.
- Transfers, payments, refunds/credits and income are classified from provider semantics before spend projection; a provider row is not automatically spend.
- Stable source identity is checked before every mutation.
- Live production writes require exact target identity, bounded batches and exact readback.
- This packet does not implement recurring/background reconciliation; that remains follow-on work after the canonical audit closes.

## Acceptance criteria

1. `M2-M1-046` is verified merged at exact base SHA with post-merge CI green.
2. Linked-account coverage/freshness is re-read before historical conclusions.
3. The provider's actual pre-projection date range, row count and account coverage are quantified without publishing private identifiers.
4. Every pre-projection provider transaction is checked against existing canonical transaction/event identity before any write.
5. Existing historical overlaps/replays are deduped rather than appended.
6. If backfill proceeds, every admitted row receives the correct money-movement/economic-event semantics and all resulting entity/relation endpoints resolve.
7. If a durable boundary is chosen instead, it is explicit, justified, query-visible and prevents later code/briefs from calling excluded history complete.
8. Duplicate event/evidence/entity/relation IDs and broken relation endpoints remain zero after mutation.
9. The live control/query guide reports the resolved historical boundary/disposition and exact readback.
10. `FIN-CANON-AUDIT-001` is either closed with evidence or left blocked by the smallest remaining concrete issue; no vague historical-backfill placeholder remains.
11. Public Git contains sanitized evidence only.

## Exact first action

1. Re-read linked-account coverage/freshness.
2. Query the provider for transactions before the current canonical lower bound.
3. Compare those source identities against the FinOps Ledger and canonical transaction entities without mutation.
4. Quantify true missing history versus replay/transfer/payment/non-spend rows.
5. Choose the smallest safe backfill or explicit boundary action and verify it by exact readback.
