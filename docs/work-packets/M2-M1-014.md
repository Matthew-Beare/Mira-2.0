# M2-M1-014 — Financial Escape budget intake and earmark reconciliation

Status: complete at bounded private-live readback evidence.

This packet was an explicit customer-priority interruption of held Android packet `M2-M1-012`. Android remained preserved at the exact provider-inspection gate recorded in `CURRENT_WORK.md`; no Android/provider state was changed by this packet.

Public Git contains only sanitized product/engineering semantics and evidence classifications. Private financial values, balances, account IDs, transaction data, household documents, spreadsheet IDs, and other operational state remain outside the repository.

## Related work

- `FIN-BUDGET-RECON-001`
- `FIN-EARMARK-001`
- existing Financial Escape private-live vertical `M2-M1-013`
- durable intake issue `#127`

## Objective

Non-destructively reconcile a newly shared household budget/income evidence source into the private Financial Escape projection while preserving existing model history and forecast assumptions, then represent user-confirmed checking earmarks, protected project reserves, recurring allowance balances, and practical spending-classification vocabulary without double counting cash.

## Scope / invariants

1. The newly shared budget/income source is evidence and a comparison/scenario input, not permission to overwrite the existing Financial Escape budget basis.
2. If source values materially conflict with the current model, preserve both and place the new source in a separate private projection tab rather than rewriting historical or forecast data.
3. Linked financial-provider balances remain provider observations; spreadsheet earmarks are cash-allocation semantics and must not alter provider balances.
4. Protected project reserves and checking earmarks must be modeled separately when their funding semantics differ, preventing double subtraction.
5. Existing transaction, daily-snapshot, normalized-spend, debt, mileage, and forecast history must remain intact.
6. User-confirmed allowance balances are forward starting balances. Monthly accrual begins on the next applicable first-of-month boundary and must not retroactively add the current month again.
7. No purchase/debit automation against allowance balances is activated in this packet; future spend reconciliation remains separate work.
8. Spending-classification vocabulary may expand without rewriting prior selections or collapsing the independent category/necessity/allowance axes.
9. All private provider/document identifiers and private values remain outside public Git.

## Acceptance criteria

- Read the new budget/income source without mutating it.
- Preserve the current Financial Escape core budget/forecast basis if replacing it would materially change existing outputs.
- Create a separate private budget/source projection when needed, with source provenance and comparison rather than destructive overwrite.
- Represent current checking earmarks and recurring allowance accruals so free/discretionary cash excludes earmarked money.
- Update the existing protected project reserve amount through its existing canonical assumption path, with downstream formulas/readback verified and no double count.
- Expand the private spending category dropdown with useful explicit categories while preserving existing values and telemetry behavior.
- Read back the affected private spreadsheet formulas/values after every write pass.
- Keep Android `M2-M1-012` recovery state exact and untouched.

## Completed evidence

1. The newly shared budget/income source was read as evidence only; no source mutation occurred.
2. The source budget materially differed from the existing MIRA planning basis, so it was preserved in a separate private source-projection tab rather than overwriting the established forecast basis.
3. The imported source-period income series reconciled exactly to the source-stated aggregate, and a private monthly income trend chart was added on the source projection.
4. A separate private earmark/allocation layer now represents checking-funded allowances and other user-confirmed set-asides without changing the provider-observed checking balance.
5. Allowance balances use user-confirmed forward starting balances, add their recurring monthly amount on later first-of-month boundaries, and intentionally do not auto-debit purchases yet.
6. The protected project reserve was changed only through the existing Financial Escape assumption path; downstream protected-target and free-cash formulas were read back successfully.
7. Free/discretionary cash now excludes checking earmarks exactly once while the protected project reserve remains separate, avoiding double subtraction.
8. The spending-category dropdown was expanded to include Entertainment plus additional practical household/financial categories; existing category values and the independent necessity/allowance axes remain intact.
9. Final readback confirmed the separate source projection, monthly aggregation, allowance accrual date handling, earmark totals, downstream free-cash treatment, and updated dropdown validation without spreadsheet errors in the touched ranges.

## Deferred work

- Purchase-to-allowance debit/reconciliation automation remains intentionally inactive until the customer explicitly authorizes that behavior.
- Any future canonical generalized implementation of these private Financial Escape semantics remains separate product work.
- Public `BACKLOG.md` normalization of the new stable work IDs can be handled as governance maintenance without reopening this private-live packet; issue `#127` preserves the sanitized durable intake in the meantime.

## Closeout

The bounded private-live refinement is complete. Restore `M2-M1-012` as the single active implementation packet at its unchanged provider-inspection hold point. Do not infer Android/provider readiness from any Financial Escape work.