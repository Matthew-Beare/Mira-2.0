# M2-M1-021 — Compute-fabric execution routing foundation

## Packet identity

- **Packet ID:** `M2-M1-021`
- **Primary work:** `LOCAL-INTEGRATIONS` (explicitly reprioritized by product-owner directive on 2026-09-08)
- **Primary features:** `LOCAL-001`, `PROVIDER-001`
- **Related features/invariants:** `SOURCE-001`, `RECOVERY-002`, `OBS-001`, `STUDIO-001`, `DEV-004`, `DIST-001`, `ONBOARD-004`, `API-001`
- **Branch:** `work/m2-m1-021-compute-fabric`
- **Base SHA:** `d4ede63db07de48504bbce2f0c5296c3a1614647`
- **Repository:** `Matthew-Beare/Mira-2.0`

## Product-owner directive captured

The 2026-09-08 compute-fabric / Studio directive is accepted as durable product direction. The whole directive is larger than one safe work packet and is therefore decomposed below rather than silently stuffed into one branch.

The accepted direction includes:

1. MIRA Studio remains the prompt-driven bounded feature generator; local compute is shared optional infrastructure, never a hard feature dependency.
2. Standard onboarding must remain usable without local infrastructure. Advanced onboarding extends the existing MIRROR-integrated interview with optional compute/server/storage/development capability registration.
3. Local workers are generic capability-advertising agents. Public source must not hard-code private hostnames, addresses, credentials, hardware identities, provider resource IDs, or private operational state.
4. Routing considers capability, privacy/policy, cost, priority, availability, health and explicit compute mode. Required modes are `off`, `normal`, and `aggressive`. Worker presence never overrides user policy.
5. Private deployment may use an always-on control-plane host plus optional burst GPU workers. Those private machine details remain deployment configuration, not public feature logic.
6. No raw inference, Python or shell service is exposed publicly. Preferred connectivity is scoped LAN/VPN or authenticated outbound worker registration behind a narrow semantic API.
7. Durable jobs must support preemption/resume metadata. Wake, heartbeat, drain, safe shutdown, do-not-wake, do-not-shutdown, interactive/gaming and maintenance/fault locks are required directions.
8. Local models are capability profiles, not product dependencies. Model/profile selection is benchmark- and evidence-driven.
9. Competitive/high-assurance Studio development may dispatch independent implementations to separate Git branches, run deterministic CI, cross-critique outputs and integrate only through reviewed work while preserving provenance.
10. AI-generated MIRROR facts normally enter as proposed/staged evidence with model/version/confidence/provenance/reconciliation state unless deterministic policy authorizes acceptance.
11. Trusted self-hosted CI must validate origin and isolate untrusted code; arbitrary public fork/PR code must not run unrestricted on trusted private machines.
12. Deterministic computation is preferred for numerical/algorithmic work. Scenario inputs must be classified as MIRROR fact, current external fact, data-derived estimate, or explicit assumption with source/as-of/confidence/uncertainty.
13. Prometheus/Grafana-compatible observability is required for worker/task health, compute/cooling telemetry, CI/tests, failures and local-versus-hosted usage.
14. Hardware safety is deterministic. Critical configured sensor or cooling faults stop affected workloads and may trigger safe shutdown. Thresholds are configuration/evidence, never invented by an LLM.
15. Hosted escalation is allowed when local quality is insufficient, work is urgent, provider interaction requires hosted execution, or higher-authority review adds value. Consumer UI automation must not be used to evade limits.
16. Human-only blockers become durable `requires-human-action` state with the minimum manual action, immediate walkthrough, verification and resume from checkpoint.
17. Optional external orchestration systems may sit behind MIRA policy but do not become MIRA's architectural center.
18. Google Sheets remains a reusable projection/control/reconciliation surface over MIRROR, not a competing authority.
19. Accepted reusable user-facing directions include Job Search core, People Discovery without automated outreach, configurable market boards, Finance Ops with both drawdown category and necessary/unnecessary classification, Vehicle Ops, Shop/Household Inventory, MIRA Ops dashboard and a Sheets prototyping lab. Education/study/skills tracking is explicitly excluded from this spreadsheet-control expansion.
20. Implementation evidence remains separated into desired, specified, implemented, test-verified, integration-verified and live-verified.

## Why this packet is bounded

The directive spans runtime routing, worker agents, durable scheduling, networking, power control, hardware safety, CI trust, deterministic analytics, Studio orchestration, onboarding, telemetry, GitHub escalation and several domain verticals. Implementing all of that in one packet would violate MIRA's packet policy and create an unreviewable mega-branch.

This packet therefore implements only the provider-neutral **execution routing contract** that downstream local/hosted workers can plug into.

## Objective

Extend the existing `PROVIDER-001` runtime router so MIRA can make a deterministic, policy-first execution-lane decision without hard-coding any private machine, model, provider or network topology.

The foundation must distinguish local versus hosted lanes, expose explicit local compute mode, require runtime capability matches, fail closed on unavailable/unhealthy/locked workers, preserve privacy/data-classification policy, and rank eligible lanes deterministically.

## Owned implementation surfaces

- `mira/runtime_router.py`
- `tests/test_runtime_router.py`
- `docs/architecture/COMPUTE_FABRIC.md`
- `docs/work-packets/M2-M1-021.md`
- this branch's `CURRENT_WORK.md`

## Shared / high-contention surfaces and integration plan

- `CURRENT_WORK.md` is branch-local and intentionally replaced for this packet.
- No `FEATURES.md`, `BACKLOG.md`, `ROADMAP.md`, `PROJECT_INSTRUCTIONS.md`, `project/code_ownership.json`, Sheets implementation file, People Discovery implementation file, finance implementation file, or Android implementation file is modified in this bounded slice.
- Existing `LOCAL-INTEGRATIONS` is used as the current machine-readable backlog anchor because the user explicitly reprioritized local-compute work and `LOCAL-001` already defines the accepted local-service boundary.
- New child work IDs and any feature-registry refinements will be added in a later governance reconciliation packet against then-current `main`, after active high-contention PRs are reconciled. The complete decomposition is preserved below so none of the accepted scope depends on chat memory.
- PR #134 currently edits `project/code_ownership.json`; PR #135 changes Sheets/code-ownership surfaces. This packet deliberately avoids those files. Before merge, re-read current `main` and preserve any newer compatible work.

## Acceptance criteria for M2-M1-021

1. Existing provider/runtime routing behavior remains backward compatible.
2. Runtime candidates can declare generic local/hosted execution kind without private machine names.
3. Local compute mode supports exactly `off`, `normal`, and `aggressive` semantics:
   - `off`: all local candidates are policy-blocked;
   - `normal`: eligible local and hosted candidates compete by deterministic priority/cost ordering;
   - `aggressive`: eligible local candidates rank ahead of hosted candidates, without overriding health, lock, capability, authorization or data policy.
4. A request can require generic runtime capabilities; candidates missing any required capability fail closed.
5. Offline, draining, faulted or unknown-health local workers cannot be selected.
6. An interactive/do-not-use lock prevents selection regardless of compute mode.
7. Existing data-classification and explicit approval policy continues to apply before runtime selection.
8. Candidate ordering does not affect the selected result.
9. No credential, host address, model secret or private deployment identifier is added to public source/tests/docs.
10. Tests cover off/normal/aggressive routing, capability mismatch, health/availability, interactive lock, policy precedence and deterministic ranking.
11. Architecture documentation defines the seam between this router and later worker-registry/scheduler/agent packets, including what is intentionally not yet implemented.
12. Exact-head CI must pass before merge. No live worker/device/provider claim is made by this packet.

## Child packet decomposition preserved for future implementation

The following are bounded follow-on outcomes, ordered by integrity/dependency rather than FIFO:

1. **Worker agent + registry** — authenticated registration, secret-free capability/health advertisements, heartbeat/freshness, worker identity, privacy/allowed-task policy and runtime evidence.
2. **Durable compute control plane** — queued task state, leases, retry/idempotency, cancellation, priority, pause/preempt/resume checkpoints and exact result provenance.
3. **Worker network/security boundary** — scoped worker identities, outbound registration/LAN/VPN patterns, narrow semantic task API, no public raw shell/Python/inference exposure.
4. **Trusted self-hosted CI** — source/origin validation, untrusted-code isolation, restricted identities, protected private runners and deterministic test evidence.
5. **Compute observability** — Prometheus-compatible worker/task metrics, job duration/failure, local-versus-hosted use and Grafana-compatible dashboards.
6. **Deterministic hardware safety** — configured sensor inventory, stale/missing-sensor behavior, warning/critical rules, stop/drain/fault/shutdown state machine and deterministic readback.
7. **Power/lifecycle control** — WOL, drain, safe shutdown, do-not-wake/do-not-shutdown, interactive/gaming and maintenance locks.
8. **Local model profiles/benchmarking** — model-agnostic profiles, benchmark evidence, quality/cost/latency/VRAM fit and versioned provenance. Initial private candidates may include current suitable gpt-oss and Qwen coding profiles, but public feature logic stays generic.
9. **Studio competitive development** — fast/normal/competitive/high-assurance modes, independent branches/worktrees, deterministic CI, cross-critique, reviewed integration and provenance.
10. **Deterministic analytics/scenario engine** — reusable Python capability plus strict source/as-of/confidence/uncertainty classifications for scenario inputs.
11. **Advanced onboarding compute extension** — extend canonical onboarding/MIRROR configuration; Standard path remains infrastructure-free.
12. **Hosted escalation + GitHub review bridge** — policy-driven escalation and durable source/CI artifacts, never consumer-UI automation to evade limits.
13. **Human-action blocker contract** — durable requires-human-action state, minimum action, walkthrough, post-action verification and checkpointed resume.
14. **Private deployment binding** — bind private always-on/control-plane and burst workers through private configuration only, preserving existing workloads and never committing private identifiers.
15. **MIRA Ops dashboard integration** — read-only/control-safe projection of worker/task/CI/telemetry state after canonical contracts exist.

The separate Sheets/Job Search/People Discovery/Finance/Vehicle/Inventory/prototyping work remains on its own packet branches or future packets and is not admitted into M2-M1-021 implementation scope.

## Session-start alignment verification — 2026-09-08

### FEATURES.md

Reviewed from verified `main` `d4ede63db07de48504bbce2f0c5296c3a1614647`. `LOCAL-001` already requires a scoped, verified local-service bridge; `PROVIDER-001` already requires provider-neutral capability routing from observed evidence; `OBS-001`, `STUDIO-001`, `DEV-004`, `DIST-001` and `ONBOARD-004` preserve the downstream observability, Studio, sanitization and onboarding directions. This packet strengthens the runtime seam without creating a second product authority.

### BACKLOG.md

Reviewed. `LOCAL-INTEGRATIONS` is currently deferred/later on `main`. The product owner explicitly reprioritized the compute-fabric direction on 2026-09-08, which is sufficient to activate a bounded packet while preserving unrelated concurrent packet branches. This packet uses the existing work ID rather than inventing an unregistered work ID mid-session.

### ROADMAP.md

Reviewed. The roadmap explicitly places advanced managed/self-hosted runtime after the ordinary Personal baseline and requires reuse of the same API/Authority semantics. Local compute remains optional and must not become a prerequisite for Standard Personal MIRA. This packet obeys that ordering by adding a reusable routing capability only.

### Collision/concurrency review

Reviewed active branches/PRs. PR #134 owns People Discovery files plus `project/code_ownership.json`; PR #135 owns Sheets control surfaces and code-ownership-fragment work. M2-M1-021 avoids those implementation surfaces.

### Direction result

ALIGNED

## Completed evidence

- Verified repository visibility is public.
- Verified `main` base SHA `d4ede63db07de48504bbce2f0c5296c3a1614647`.
- Verified existing runtime router, service-state capability evidence, onboarding, roadmap, features, backlog and concurrency policies before implementation.
- Created isolated branch `work/m2-m1-021-compute-fabric` from the exact verified base.

## Exact next action / resume point

1. Replace branch `CURRENT_WORK.md` with this packet's exact state.
2. Add architecture boundary document.
3. Implement bounded execution-lane metadata and policy in `mira/runtime_router.py` without provider/device I/O.
4. Extend deterministic tests in `tests/test_runtime_router.py`.
5. Run/obtain exact-head CI, reconcile against current `main`, then merge only if green and non-destructive.
