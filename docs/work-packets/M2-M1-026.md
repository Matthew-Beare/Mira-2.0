# M2-M1-026 — Trusted self-hosted CI boundary

## Packet identity

- **Packet ID:** `M2-M1-026`
- **Primary work:** `LOCAL-INTEGRATIONS`
- **Primary features:** `LOCAL-001`, `DEV-004`
- **Related invariants/features:** `DEV-001`, `RECOVERY-002`, `DIST-001`, `API-001`
- **Branch:** `work/m2-m1-026-trusted-self-hosted-ci`
- **Base SHA:** `c6692b014cc17508e8334e2b524b64b428857b21`
- **Repository:** `Matthew-Beare/Mira-2.0`
- **Pull request:** `#141`

## Objective

Create the deterministic trust seam that a later private self-hosted runner must consume before executing repository source. Arbitrary public fork/PR code must never become private-runner eligible merely because GitHub delivered an event or CI passed somewhere.

The packet separates two facts:

1. **Source trust** — whether the exact source commit is a successful canonical-repository `push` to the canonical branch from the canonical repository.
2. **Runner isolation** — whether the prospective runner is independently restricted enough for source execution.

A source-trusted commit without runner-isolation evidence stops at `requires_isolation`. Pull-request/fork source is `hosted_only` regardless of any runner-isolation claim.

`RunnerIsolationEvidence` is a trusted-observer contract, never candidate/runner self-attestation. A later private deployment must derive it from the sandbox, identity and network controls that deployment actually enforces.

## Bounded implementation

1. `project/ci_trust.py` defines secret-free workflow provenance, source trust, runner isolation, deterministic decisions and JSON receipts.
2. Exact canonical repository, branch, source SHA, policy SHA, workflow/run identity and machine-readable reason codes are preserved in receipts.
3. Runner isolation is an independent trusted-observer contract requiring restricted identity, ephemeral workspace, sandboxed source execution, no injected credentials and no private-LAN access for the minimum synthetic eligibility profile.
4. Only trusted canonical-main source plus safe isolation can evaluate to `private_runner_eligible`.
5. `.github/workflows/trusted-runner-gate.yml` is triggered by completion of the existing `CI` workflow. The gate itself runs on GitHub-hosted infrastructure only, uses `contents: read`, checks out policy from the default branch rather than the triggering candidate SHA, and emits a preflight receipt artifact.
6. The GitHub-hosted gate intentionally supplies no runner-isolation evidence, so its successful trusted-main receipt remains `requires_isolation`; it is evidence for a later private scheduler, not authorization by itself.
7. Existing `.github/workflows/ci.yml` remains unchanged and continues to be the first-line execution environment for PR/fork code.
8. `tests/test_ci_trust.py` covers source/origin, workflow/conclusion, runner-isolation, deterministic serialization, CLI preflight and workflow-shape invariants.

## Security/integrity rules

- No `pull_request_target` execution of candidate source.
- No `self-hosted` runner target is introduced by this packet.
- No repository secret is required by the preflight workflow.
- No private hostname, IP, runner label, hardware identity, LAN/VPN detail, credential or provider resource ID enters public source.
- A green PR is not equivalent to trusted private-runner source.
- A same-repository PR is still PR source and remains hosted-only.
- A successful push to a noncanonical branch remains hosted-only.
- Wrong repository/origin, failed CI, malformed SHA/provenance or unsupported event state fails closed.
- Runner isolation cannot upgrade untrusted source.
- Source trust cannot waive runner isolation.
- Runner-isolation evidence must be created by a trusted deployment observer from actually enforced controls; source or runner self-report is not valid evidence.
- The preflight receipt is evidence, not a bearer capability or scheduling token.
- Later private execution must independently read and validate both exact source trust and current runner-isolation evidence before scheduling.

## Acceptance evidence target

- Unit tests prove fork PR, same-repo PR, wrong branch/repository, failed CI and malformed provenance cannot become private-runner eligible.
- Unit tests prove a canonical successful main push still requires isolation when no runner evidence is supplied.
- Unit tests prove unsafe isolation is blocked and safe isolation permits only already-trusted source.
- Receipt serialization is deterministic and secret-free.
- Workflow-shape tests prove the preflight uses GitHub-hosted execution, read-only permissions, no secrets, no `self-hosted`, no `pull_request_target`, and does not check out the triggering candidate SHA.
- Existing CI remains green on the exact PR head.
- Merge/post-merge CI must pass before integration verification is claimed.
- No live self-hosted runner, sandbox, private credential or LAN execution is claimed.

## Evidence so far

- M2-M1-025 merged as PR #140 at `c6692b014cc17508e8334e2b524b64b428857b21`; post-merge CI #533 passed end-to-end.
- M2-M1-026 branch and PR collision checks found no existing packet claim.
- Existing CI is GitHub-hosted only with read-only content permission and no self-hosted job.
- Repository search found no prior self-hosted source/origin trust gate.
- Local policy checks passed before commit for canonical main, safe/missing/unsafe isolation, same-repository PR, fork PR, non-main push, failed CI, wrong workflow and malformed provenance.
- Branch diff from base contains only this packet's current-work/packet docs, trust policy, trust tests and hosted preflight workflow.
- Draft PR #141 opened from exact implementation head `e7bbdeed24485c2e9681f10c079fd97a8988e844`.
- CI #534 on that exact implementation head passed end-to-end, including compile, lifecycle/alignment, code ownership, Android proof, Python trust-policy tests and Workspace tests.
- A subsequent code/documentation hardening checkpoint makes the trusted-observer requirement explicit. Final exact-head CI on that closeout head is still required before merge.

## Explicitly deferred

- registering a real private runner;
- choosing private runner labels or machine bindings;
- container/VM sandbox implementation;
- network namespace/firewall enforcement;
- private credential brokering;
- actual source execution on private hardware;
- Wake-on-LAN/power lifecycle;
- model/runtime execution;
- private telemetry binding.

Those require private deployment evidence and later bounded packets.

## Exact resume point

Run final exact-head CI on the trusted-observer/documentation closeout head. If green, reconcile current `main` and PR #141, mark ready, merge with expected-head protection, then require post-merge main CI before claiming integration verification. Do not add a self-hosted job to this packet.
