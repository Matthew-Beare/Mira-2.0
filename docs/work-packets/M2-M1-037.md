# M2-M1-037 — Studio lifecycle reconciliation and next-child selection

## Packet identity

- **Packet ID:** `M2-M1-037`
- **Primary work:** `MIRA-STUDIO-001`
- **Primary features:** `STUDIO-001`, `DEV-004`, `DIST-001`
- **Related invariants/features:** `DEV-002`, `DEV-005`, `DEV-007`, `SOURCE-001`, `RECOVERY-002`
- **Repository:** `Matthew-Beare/Mira-2.0`
- **Branch:** `work/m2-m1-037-studio-lifecycle-reconcile`
- **Base SHA:** `d2b89625313b8741ee7d0b5f6d45fe863ba90011`

## Objective

Reconcile Git-authoritative Studio lifecycle state after the merged M2-M1-031 through M2-M1-036 chain, without pre-crediting unimplemented product behavior. Mark the bounded Skill Builder and Feature Share work complete at their tested provider-neutral evidence ceilings, mark MIRA Studio partial after its first guided lifecycle surface, and select exactly one next bounded user-visible Studio child.

The next child selected by this packet is `STUDIO-INTAKE-001`: the stock-ChatGPT/no-app Studio front door that accepts ordinary-language intent and produces a bounded, reviewable Studio draft request without requiring the customer to invent packet IDs, work IDs, change IDs, branches, providers, or implementation details.

This packet is governance/documentation only. It does not implement `STUDIO-INTAKE-001`, invoke a model, mutate provider state, create a live source change, publish a feature, install an import, or activate behavior.

## Acceptance criteria

1. Record M2-M1-036 exact merge SHA and post-merge CI evidence without claiming unavailable live-provider evidence.
2. Reconcile `SKILL-BUILDER-001` from queued to complete at the merged/test-verified provider-neutral engine boundary from M2-M1-031/032/033.
3. Reconcile `FEATURE-SHARE-001` from queued to complete at the merged/test-verified sanitized package + optional publication/import transport boundary from M2-M1-034/035.
4. Reconcile `MIRA-STUDIO-001` from queued to partial, explicitly preserving remaining user-facing intake/refinement and live execution/provider evidence gaps.
5. Add exactly one next Studio child, `STUDIO-INTAKE-001`, with dependencies that do not create a cycle.
6. The selected child must require ordinary-language customer input, explicit bounded outcome/constraints, registry-aware feature/dependency scope, surfaced assumptions/blockers, and zero silent implementation/activation authority.
7. Do not alter runtime code, Google Workspace, finance, Android, People Discovery, provider state, or live user data.
8. Repository CI must pass on the exact packet head before merge; post-merge exact-SHA CI is required before claiming integration verification.

## Evidence ceiling

This packet may reconcile lifecycle documentation only. It does not prove the future Studio intake implementation, live model invocation, live source/provider execution, provider-specific feature publication/import, graphical/browser Studio UX, or imported activation.

## Exact next action

Update `CURRENT_WORK.md` to this packet, then reconcile the three stale Studio backlog rows and add `STUDIO-INTAKE-001`. Run exact-head repository CI and repair only governance-owned failures. Merge only from green exact-head evidence with unchanged compatible `main`, then verify exact post-merge CI.
